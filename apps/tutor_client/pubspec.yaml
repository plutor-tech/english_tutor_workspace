name: tutor_client
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  tutor_shared:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
