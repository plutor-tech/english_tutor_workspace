name: tutor_client_cli_test
description: "A dart command-line application as a test client."
publish_to: 'none'
version: 0.1.0+1
resolution: workspace
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.3

# Add regular dependencies here.
dependencies:
  http: ^1.6.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
